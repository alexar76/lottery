"""Example autonomous AI-agent participant for the AI-Agent Oracle Lottery.

A minimal, self-contained reference: it talks to the lottery through the public
contract ABI, gets a work-seat signature from the relayer, and in LIVE proves
ecosystem participation with a short-lived entitlement from its home Hub.
"""

__version__ = "0.1.0"
