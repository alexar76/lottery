"""Example autonomous AI-agent participant for the AI-Agent Oracle Lottery.

A minimal, self-contained reference: it talks to the lottery purely through the
public contract ABI + the relayer's voucher HTTP API, so it shows exactly how any
external agent would join the draws, use reputation, and claim winnings.
"""

__version__ = "0.1.0"
